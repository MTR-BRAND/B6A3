# Vehicle Rental System Documentation

## প্রজেক্ট সম্পর্কে
এই প্রজেক্টে একটা গাড়ি ভাড়ার সিস্টেম বানানো হয়েছে। এখানে কাস্টমাররা গাড়ি বুক করতে পারবে আর আমরা সব রেকর্ড রাখতে পারব।

## ডাটাবেসে কি কি টেবিল আছে

আমাদের ডাটাবেসে তিনটা টেবিল আছে:

- **bookings_table** - এখানে বুকিং এর সব তথ্য থাকে
- **usre_table** - এখানে কাস্টমারদের তথ্য রাখা হয়
- **vehicles_table** - এখানে গাড়ির লিস্ট থাকে

## কুয়েরি গুলো কি কাজ করে

### ১ নাম্বার কুয়েরি - সব বুকিং দেখা

এই কুয়েরি দিয়ে আমরা সব বুকিং এর পুরা তথ্য দেখতে পারি। কোন কাস্টমার কোন গাড়ি কবে বুক করছে সব দেখা যায়।
```sql
SELECT bookings_table.booking_id,
  usre_table.name AS customer_name, 
  vehicles_table.name AS vehicle_name,
  bookings_table.start_date,
  bookings_table.end_date, 
  bookings_table.status
FROM bookings_table
INNER JOIN usre_table 
  ON bookings_table.user_id = usre_table.user_id 
INNER JOIN vehicles_table 
  ON bookings_table.vehicle_id = vehicles_table.vehicle_id;
```

**কখন এটা লাগবে:**
- কাস্টমারের আগের বুকিং দেখতে
- রিপোর্ট বানাতে
- কোন বুকিং এখন কি অবস্থায় আছে দেখতে

---

### ২ নাম্বার কুয়েরি - যেসব গাড়ি কখনো ভাড়া হয় নাই

এই কুয়েরি দিয়ে বের করা যায় কোন গাড়িগুলা এখনো কেউ ভাড়া নেয় নাই। এটা দেখে আমরা বুঝতে পারব কোন গাড়ি জনপ্রিয় না😁।
```sql
SELECT *
FROM vehicles_table AS v
WHERE NOT EXISTS (
  SELECT *
  FROM bookings_table AS b
  WHERE b.vehicle_id = v.vehicle_id
) 
ORDER BY v.vehicle_id ASC;
```

**কখন এটা লাগবে:**
- কোন গাড়ি কেউ নিচ্ছে না সেটা বুঝতে
- মার্কেটিং করার জন্য
- নতুন গাড়ি কিনবো কিনা সিদ্ধান্ত নিতে

---

### ৩ নাম্বার কুয়েরি - এখন কোন গাড়ি available আছে

এই কুয়েরি দিয়ে দেখা যায় এখন কোন গাড়ি ভাড়া দেওয়ার জন্য ready আছে।
```sql
SELECT * 
FROM vehicles_table 
WHERE vehicles_table.type = 'car' 
  AND vehicles_table.status = 'available';
```

**কখন এটা লাগবে:**
- কাস্টমার যখন গাড়ি খুঁজবে
- ওয়েবসাইটে available গাড়ি দেখাতে

---

### ৪ নাম্বার কুয়েরি - কোন গাড়ি বেশি জনপ্রিয়

এই কুয়েরি দিয়ে দেখা যায় কোন গাড়ি সবচেয়ে বেশি বুক হয়। এখানে যেসব গাড়ি ২ বারের বেশি বুক হইছে সেগুলা দেখায়।
```sql
SELECT 
  v.name AS vehicle_name,
  COUNT(b.booking_id) AS total_bookings
FROM vehicles_table v
JOIN bookings_table b 
  ON v.vehicle_id = b.vehicle_id
GROUP BY v.name
HAVING COUNT(b.booking_id) > 2;
```

**কখন এটা লাগবে:**
- কোন গাড়ির demand বেশি সেটা বুঝতে
- দাম বাড়াবো কিনা ঠিক করতে
- এই টাইপের গাড়ি আরো কিনবো কিনা ভাবতে

---

## শেষ কথা

এই ডকুমেন্টেশনে আমার প্রজেক্টের মূল কুয়েরিগুলা explain করা হইছে।

---
## Author
**Arafat Raz**  
Backend Developer

